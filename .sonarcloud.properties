# SonarCloud Automatic Analysis configuration
#
# Marketing pages are content-driven: each page.tsx pairs a same-shaped
# metadata block with arrays of {title, description, points} copy. Sonar's
# copy-paste detector anonymizes string literals, so these register as
# duplicated code even though only the structure repeats. Exclude pages from
# duplication detection only — all other rules still apply to these files.
#
# lib/site-routes.ts is the same case in a different shape: one entry per
# page, each with the same keys and different copy. It exists precisely to
# stop that copy being duplicated for real across the sitemap, /llms.txt and
# the share cards, so collapsing its entries further to satisfy the detector
# would trade a readable table for a worse one.
sonar.cpd.exclusions=app/**/page.tsx,lib/site-routes.ts
